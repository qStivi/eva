# Eva — Status & Resume Point (2026-06-13)

Checkpoint of where Eva stands — **memory now works** (the core milestone), plus two model
evaluations (gauntlet #1: 30B-A3B Q3/Q4/Q6 + Qwen3.5-9B; gauntlet #2 on 2026-06-14: the gpt-oss /
GLM / DeepSeek-R1 / Ministral / LFM2 haul). Supersedes the pre-Letta `CURRENT_STATUS.md`.

**Latest (2026-06-14):** gauntlet #2 found **gpt-oss-20b** is a clear daily-driver upgrade over qwen3-8b
(60 t/s, fits VRAM, faithful proactive saves) — **decided but NOT yet applied** (keeper still on qwen3-8b).
See "Model evaluation #2" below. New reusable harness in `scripts/{run-candidate,gauntlet,tps}.sh`.

## TL;DR of the big pivot
Eva is now built **on Letta** (self-hosted agent + memory framework), brain =
**local LM Studio models**, model-agnostic so the brain is swappable (decision "option C":
local-default, borrow a frontier model only if needed). Full reasoning in `docs/2026-06-REVIEW.md`.

---

## What's running right now (all boot-persistent)
| Piece | What/where | Manage |
|---|---|---|
| **Letta** | rootless Quadlet unit, API `http://localhost:8283` (`/docs` = Swagger). Data in podman volume `letta_pgdata`. | `systemctl --user {status,restart} letta` |
| **Brain** | LM Studio OpenAI server `:1234` (`autoStartOnLaunch=true`, JIT on, binds 0.0.0.0). Models: `qwen/qwen3-8b`, `text-embedding-nomic-embed-text-v1.5`. | LM Studio app (autostarts to tray) |
| **eva-web** | LAN chat UI, systemd user unit, `http://192.168.128.126:8284` (Basic auth, creds in `~/.config/eva-web/eva-web.env`). "Just a testing interface." | `systemctl --user {status,restart} eva-web` |
| **eva CLI** | `~/.local/bin/eva "msg"` — chat from any terminal. | — |
| **autostart→tray** | `~/.local/bin/launch-to-tray` + `~/.config/autostart/*.desktop` for LM Studio & Spotify (KWin script closes window to tray). | — |

### Letta config gotchas (already solved, don't re-derive)
- LM Studio API-key auth is **enforced** → Letta uses the **OpenAI-proxy provider**
  (`OPENAI_API_BASE`+`OPENAI_API_KEY` in `~/.config/letta/letta.env`), NOT the keyless LMSTUDIO provider.
- Letta does **not** auto-register local embedders → keeper agent is created with an explicit
  `embedding_config` pointing at nomic (768-dim). See `scripts/create-eva.sh`.

### Agents
- **`eva`** = `agent-87f7dbff-5171-40b9-adcb-3402af1e36c8` — the **keeper** (real persona). CLI + web point here.
- **`eva-spike`** = `agent-5641f94a-1aff-443b-a1e8-066bfd3677ed` — throwaway test/spicy agent.
  Reach it with `EVA_AGENT_ID=agent-5641… eva "..."`.

### Personality workflow ("on the side")
- `persona/eva.md` — version-controlled keeper personality (journal-companion Eva: ~70% sass / 30% care).
- `scripts/create-eva.sh` — (re)create the keeper from the persona file. `EVA_MODEL=...` overrides the model.
- `scripts/sync-persona.sh <agent_id>` — hot-push persona edits into a running agent (no restart).
- Iterate loop: edit `persona/eva.md` → `scripts/sync-persona.sh agent-87f7dbff…` → `eva "..."`.

---

## Memory — ✅ SOLVED (2026-06-13)
The earlier "memory isn't consolidating" problem is fixed. It was **never a model-capability issue**
(the model tool-calls fine, reasons before acting — `source: reasoner_model` — and saves correctly
when asked). It was a **triggering** issue. Fix: a persona directive tying the save to Eva's
in-character **"scribble in my notebook"** gesture — the scribble *is* a real `memory_insert` to the
`human` block (+ honesty/perspective guards). See `persona/eva.md`.

Shipped on **qwen3-8b**: she saves durable user facts **proactively**, and it **persists across a
Letta restart and is recalled** (verified end-to-end). Memory-hygiene scorecard:

Shipped on **qwen3-8b** (see the full evaluation below for why).

---

## Model evaluation (full 2026-06-13 gauntlet)
Each tested as Eva's brain via Letta — same quality conversation + memory checks + speed benchmark.
**Usable VRAM ≈ 14.9 GB** (desktop uses ~1 GB), so any 30B *always* needs CPU offload.

| Model | Decode | Saves on its own | Faithful | Perspective | Confab (chat) | Voice |
|---|---|---|---|---|---|---|
| **qwen3-8b** (fits VRAM) | **~36 t/s** | ✅ | ✅ | ❌ 1st-person | low (literal) | plain, loops |
| 30B-A3B-Instruct **@q3_k_l** | 36 t/s | ❌ **never fired** | — | — | ✅ worst | excellent |
| 30B-A3B-Instruct **@q4_k_m** | 19 t/s | ✅ | ✅ | ✅ 3rd-person | ⚠️ banter only (not saved) | **richest** |
| 30B-A3B-Thinking @q4_k_m | ~37 t/s* | ✅ | ✅ | ✅ | low | rich (+~7s think pause) |
| Qwen3.5-9B **@q8_0** | 25 t/s + thinking | ✅ | ✅ | ❌ 1st-person | low | ok, muddled |

\* same decode rate, but ~240 reasoning tokens/reply → ~8 s felt latency before the answer.

**Findings:**
- **Q3 quant breaks tool discipline** — 30B-Q3-Instruct *never* fired `memory_insert` AND confabulated a fake
  history. Q4 fixed it (real saves, 3rd-person, faithful). The quant matters for memory mechanics.
- **Confabulation in *banter* is partly inherent** to eloquent models (Q4-30B invents shared history as flavor)
  — but it does **not** poison the saved journal. Needs a guardrail (see NEXT).
- **3rd-person perspective: only the 30B got it right.** 8B and Qwen3.5-9B both write 1st-person → tracks with
  capability, not recency or thinking (the 8B already thinks and still writes 1st-person).
- **Qwen3.5-9B underwhelmed** — slower than the 8B (Q8 is bandwidth-heavy + new Gated-DeltaNet kernels
  unoptimised), thinking-on by default (`/no_think` doesn't disable it), 1st-person, muddled prose. Loads/runs
  fine though. Shelve until llama.cpp optimises; a fair retest = Q4_K_M + thinking-off.
- **Speed mechanics:** prefill ~280 t/s (uncached); LM Studio **caches the prompt prefix across turns**, so
  ongoing chat only re-processes new tokens (fast) — cold start / changed context pays the prefill.

**Decision (daily brain is a genuine speed-vs-quality trade):**
- **Daily driver = `qwen3-8b`** (fast, fits VRAM, reliable saves; accept 1st-person + loops for now). ← shipped.
- **Quality option = 30B-A3B-Instruct @q4_k_m** (richest voice + correct 3rd-person memory; 19 t/s; needs the
  confab guardrail). Revisit as daily driver after the **X3D CPU upgrade** speeds the offload path.
- **Sleep-time consolidator = 30B-A3B-Thinking @q4_k_m** (faithful + reasoning; latency irrelevant in background).
- **Avoid:** Q3 (broken memory); Q6/Q8 (slowest, no real quality gain on 16 GB); Qwen3.5-Q8 (slow + thinky).

**Models on disk** (`~/.var/app/ai.lmstudio.lm-studio/.lmstudio/models/lmstudio-community/`): qwen3-8b, nomic;
30B-A3B **Instruct & Thinking** each in **Q3_K_L / Q4_K_M / Q6_K**; **Qwen3.5-9B Q4_K_M/Q6_K/Q8_0**; plus the
2026-06-14 haul: **gpt-oss-20b** (MXFP4), **GLM-4.6V-Flash** (Q4/Q8 +vision), **DeepSeek-R1-0528-Qwen3-8B**
(Q4/Q8), **Ministral-3-14B-Reasoning** (Q4/Q6/Q8 +vision), **Ministral-3-3B-Instruct** (Q4/Q8), **LFM2-24B-A2B**
(Q4), **gemma-4-E4B-it** (Q4 +vision), **LFM2.5-1.2B-Instruct** (Q8).

---

## Model evaluation #2 — the 2026-06-14 haul (gpt-oss + GLM + DeepSeek-R1 + Qwen3.5 + Ministral + LFM2)
Same gauntlet, run via new reusable harness `scripts/{run-candidate,gauntlet,tps}.sh` (creates a throwaway
agent on the model, runs a fixed 4-turn convo, dumps the human block, deletes the agent, then probes decode t/s).

| # | Model | Size/quant | Decode | Saves on its own | Faithful | Perspective | VRAM | Voice |
|---|---|---|---|---|---|---|---|---|
| **1** | **gpt-oss-20b** | MXFP4 12.1 GB | **60 t/s** | ✅ | ✅ | ⚠️ **2nd**-person | ✅ fits | good snark |
| **2** | **Qwen3.5-9B** | Q6 (default) | 29 t/s | ✅ | ✅ | ✅ **3rd-person** | ✅ fits | ok, thinky |
| 2b | Qwen3.5-9B **Q4_K_M** | Q4 6.1 GiB | **28.9 t/s** | ✅ | ⚠️ confab | ✅ 3rd-person | ✅ fits | ok; **dup writes** |
| 3 | Ministral-3-14B-Reasoning | Q4 9 GB | 38 t/s | ❌ narrates | — | — | ✅ fits | **richest** |
| 4 | GLM-4.6V-Flash | ~Q6 7.4 GB | 54 t/s | ❌ never fired | — | — | ✅ fits | decent |
| 5 | DeepSeek-R1-0528-Qwen3-8B | Q4 5 GB | **72 t/s** | ❌ narrates | ✗ confab | — | ✅ fits | rambly, loops |
| 6 | LFM2-24B-A2B | Q4 14.4 GB | 48 t/s | ❌ claims-only | ✗ | — | offload | off-character (emoji) |

**The decisive finding — only 2 of 6 actually persist memory.** gpt-oss-20b and Qwen3.5-9B fire `memory_insert`;
the other four **perform the "scribble in my notebook" gesture as roleplay prose** and never call the tool, so the
human block comes back **empty**. The split is clean by model class: **reasoning-distills** (DeepSeek-R1,
Ministral-Reasoning) and the **vision model** (GLM-4.6V) narrate the scribble; only true **instruct/agentic** models
(gpt-oss, the Qwen instruct line) call it. → The scribble directive is a *trigger* that only fires on tool-disciplined
models; reasoning/vision tunes treat it as something to act out. (Mirrors the earlier Q3-30B "never fired" result.)

**Other findings:**
- **DeepSeek-R1-8B** also confabulated (invented "Leo who loves horror movies and terrifies everyone") and looped a
  whole paragraph verbatim. Fast but unusable.
- **LFM2-24B** broke character entirely into an emoji-laden helpful-assistant ("No judgment, just sass 🐾") AND
  falsely claimed "I've got Stephan saved in there." Worst persona adherence + confabulated its own memory.
- **Ministral-3-14B-Reasoning** had the richest voice of the whole bunch but won't persist — a chat-flavor model, not a keeper.
- **Qwen3.5-9B Q4_K_M retest (2026-06-14):** the default gauntlet ran **Q6** (LM Studio auto-picks the recommended
  variant; `lms load` has no quant selector and the grouped key has no `@quant` handle — forced Q4 by temporarily
  stashing the Q6/Q8 GGUFs out of the folder). **Q4 = 28.9 t/s, identical to Q6's 29.1** → the slowness is
  **architectural** (thinking + unoptimized Gated-DeltaNet kernels), NOT quant bandwidth, so "Q4 + thinking-off" is a
  dead end (Q4 no faster, thinking can't be disabled). Q4 kept 3rd-person but was **messier than Q6** — wrote the intro
  line 3× (duplicate `memory_insert`s; it even noticed and didn't fix it) and confabulated a fake "last week we talked"
  memory. Verdict: Q4 is not an upgrade; Qwen3.5-9B stays off the daily list at any quant. Dup-write hygiene is a yellow
  flag even for the consolidator role.

**Decision (APPLIED 2026-06-14 — keeper `agent-87f7dbff` now on `openai-proxy/openai/gpt-oss-20b`):**
- Promotion done via in-place `PATCH` (no rebuild). Persona synced (`sync-persona.sh`) with the new 3rd-person +
  confab guards; verified a `memory_insert` save in third person ("Stephan takes his coffee black") and that model +
  saved fact **survive a `systemctl --user restart letta`**.
- **gpt-oss-20b is a genuine daily-driver upgrade over qwen3-8b**: **60 t/s vs 36** (1.7×), fits VRAM with no offload,
  keeps proactive + faithful saves, perspective improves **1st→2nd person** (nudgeable toward 3rd via a persona guard),
  good in-character snark, no looping. When promoted: switch `agent-87f7dbff…` to `openai-proxy/openai/gpt-oss-20b`
  (recreate via `EVA_MODEL=openai-proxy/openai/gpt-oss-20b scripts/create-eva.sh`, or repoint the existing agent) and
  add a 3rd-person nudge to `persona/eva.md`, then verify a save survives a Letta restart.
- **Qwen3.5-9B** is the only candidate that nails **3rd-person + faithful**, but 29 t/s + unkillable thinking make it a
  poor foreground brain. Reconsider it as the **sleep-time consolidator** (fits VRAM, correct perspective) vs 30B-Thinking.
- **Drop for Eva:** GLM-4.6V, DeepSeek-R1-8B, Ministral-Reasoning, LFM2-24B (all fail to persist). The small vision
  models (gemma-4-E4B, Ministral-3-3B) were not gauntleted — keep as future vision/aux, not keeper brains.

### Harness + ops gotchas from this round (don't re-derive)
- **`lms load` rejects the `@quant` selector** (`model@q4_k_m` → "no such model"); load the **bare key** and LM Studio
  picks its default variant — read the logged `(N GiB)` size to infer the quant. (The `@quant` handles only work as
  *Letta model ids*, not as `lms load` keys.)
- **Letta caches its model list and only refreshes on restart.** After downloading new models, `systemctl --user
  restart letta`, then they appear in `GET /v1/models/` by canonical handle (`openai-proxy/<publisher>/<model>`).
  A custom `lms --identifier` does NOT surface in Letta's list (LM Studio's `/v1/models` reports canonical ids only),
  so addressing by real handle is the reliable path — no identifier trick needed.
- **Live model switch works in place (verified 2026-06-14, throwaway agent).** `PATCH /v1/agents/{id}` with
  `{"model":"openai-proxy/<pub>/<model>"}` swaps the brain **without recreating the agent** — memory blocks
  (persona/human) and base tools (`memory_insert`/`memory_replace`/`conversation_search`) all survive, and LM Studio
  **JIT-loads** the new model on the next turn. Confirmed end-to-end: switched qwen3-8b→gpt-oss-20b, agent fired a
  proactive `memory_insert` and the human block updated. **Caveat:** `context_window` is NOT re-derived per model — the
  PATCH keeps the original agent's cap (30000 here), so a switch doesn't auto-grow context for a bigger model. This is
  the capability any future model-switcher UI (Flutter, planned) would call — no agent rebuild needed.

**Model ops gotchas:** download via **hf-mirror.com** + `aria2c -x4` (HF throttles — see [[hf-mirror...]] memory).
Download into `~/model-staging` then `mv` into the models dir — a *growing* file inside LM Studio's watched
folder makes its UI lag. Address models by **explicit `@quant`** (bare id is ambiguous → picks a half-downloaded
quant → crashes llama-server). I can drive `lms load <model> --gpu <ratio> -c <ctx> --parallel <n> --ttl <s>`
+ `lms unload --all` via the flatpak; advanced knobs (MoE-CPU offload count, KV-cache quant, Flash Attn) come
from the model's **saved GUI config**, not the CLI.

### Memory-fix recap

Timestamps (matters for "wasn't that last week?"): **messages** have a `date`; **core-memory
blocks/facts have NO timestamp**; **archival** passages are timestamped by design → store
episodic/dated memory in archival, stable facts in core (maps onto the future Logseq journal layer).

---

## NEXT (priority order)
0. ~~**Promote gpt-oss-20b to daily driver.**~~ **✅ DONE 2026-06-14.** Keeper `agent-87f7dbff` PATCHed in place to
   `openai-proxy/openai/gpt-oss-20b`; `persona/eva.md` got the 3rd-person nudge (now forbids 2nd-person too) + a
   confab guard, synced to the live keeper; save verified in third person and persistent across a Letta restart.
   Model-switch is in-place (no rebuild) — see the "Live model switch" gotcha above.
1. **Sleep-time consolidation agent (the big one).** A background Letta agent (`enable_sleeptime`)
   running **30B-A3B-Thinking** that, while idle, cleans up the 8B's memory: fix 1st→3rd person,
   strip embellishment, dedupe, and **route episodic/dated facts into timestamped archival**. Closes
   both the perspective wart AND the temporal-memory gap. Architecture: fast foreground brain,
   capable thinking model in the background. **Consolidator candidates:** 30B-A3B-Thinking @q4_k_m (faithful,
   needs offload) vs **Qwen3.5-9B** (gauntlet #2: nailed 3rd-person, fits VRAM, latency irrelevant in background —
   but Q4 retest showed **duplicate-write hygiene issues**, so vet that before trusting it to consolidate) —
   A/B these for the background role.
2. **Confabulation guardrail (quick win, NOT yet done).** Add to `persona/eva.md`: "only reference what's
   actually in your memory; never invent shared history." Eloquent models fabricate "remember when you…"
   stories in banter; this reins it in. Helps every model.
3. **Looping/parroting** on the 8B → LM Studio **repeat penalty** (~1.2–1.3) + temp tune.
4. **Tools** (roadmap below): web search → scoped journal → shell behind guardrails.

Note: user will **wipe Eva's memory before going live**, then use throwaway agents for testing
(so it's fine to test directly on the keeper until then).

---

## Tools & guardrails roadmap (after memory works)
Risk ladder — build in this order, least privilege:
1. ~~**Web search**~~ **✅ DONE 2026-06-14.** SearXNG MCP (`:3010`) wired into Letta; keeper now has
   `searxng_web_search` + `web_url_read`. Verified end-to-end (Eva searched live and cited the source).
   See the "Letta MCP wiring" gotcha below.
2. **Scoped journal/Logseq tools** — read/append/search the journal dir only (NOT generic file edit). The real vision.
3. **Shell** — only behind: sandbox (Letta container/distrobox, read-only mounts) + deny-rules + **HITL approval**
   (Letta natively supports "tool requires approval" — agent pauses; our client must handle the approval msg type).

**Classifier idea (like auto-mode):** Llama Guard 3/4 is self-hostable, tuned for tool calls, screens in/out,
includes Prompt Guard (injection detection). Use the small **Guard-3-1B** so it doesn't fight VRAM. Its
highest-value job is **prompt-injection screening once Eva ingests web/journal content** — not vetting our own
commands. It's one layer, not the whole defense.

### Letta MCP wiring gotcha (don't re-derive — 2026-06-14)
Wiring the local SearXNG MCP (`127.0.0.1:3010/mcp`, streamable-HTTP) into Letta 0.16.8:
- **Register via the NEW route** `POST /v1/mcp-servers/` with
  `{"server_name":"searxng","config":{"mcp_server_type":"streamable_http","server_url":"http://127.0.0.1:3010/mcp"}}`.
  Then per tool: `POST /v1/tools/mcp/servers/searxng/<tool_name>` (persists it as a Letta tool) →
  `PATCH /v1/agents/<id>/tools/attach/<tool_id>`. Tools exposed: `searxng_web_search`, `web_url_read`.
- **The blocker:** Letta's `helpers/url_validation.py` rejects **every non-global IP** for MCP URLs — loopback AND
  private LAN both fail (`Non-public IP not allowed`). There is **no env/setting** to relax it (only `mcp_disable_stdio`
  exists, and stdio is unusable — the letta image has no node/npx to spawn the searxng MCP). Registration uses
  `resolve_hostname=False` so a bare hostname slips through, but the **connect/execute path re-resolves and re-blocks**,
  so alias tricks don't help.
- **The fix:** bind-mount a patched validator over that module. Files (live in `~/.config`, update-safe, NOT in this repo):
  `~/.config/letta/url_validation.py` (permits loopback/private, still blocks link-local/cloud-metadata `169.254`/`fe80`)
  and `~/.config/containers/systemd/letta.container` (`Volume=…url_validation.py:…:ro,Z`). Re-apply after a Letta image
  bump if upstream moves/renames that module. Full rationale appended to `~/lmstudio-searxng-websearch.md`.

---

## Decisions captured from this planning session

### Using the 46 GB system RAM (vs 16 GB VRAM)
Two genuinely useful uses:
1. **Run a model bigger than VRAM via CPU offload — especially a MoE.** e.g. **Qwen3-30B-A3B** (MoE, only
   ~3B active params) at Q4 (~18–20 GB): offload spillover layers to RAM; stays fast *because* MoE activates
   few params. Net: a noticeably smarter Eva than dense-14B, at acceptable speed. Worth A/B-ing against 14B.
2. **Pin auxiliary models to CPU/RAM** so the GPU is 100% the chat model: run the **nomic embedder** and a
   **Llama-Guard-1B** with GPU-offload=0 (CPU). 46 GB easily holds several resident models simultaneously
   (chat on GPU + embeddings + guard + a future sleep-time consolidation model on CPU) with no VRAM contention.

### Making Eva's file edits undoable (for the future file/journal tool)
**Make her writable area a git repo and have the file tool auto-commit each change.** Then every edit is a
revertible commit with a diff + history — natural fit for a journal, and matches your "git keeps the receipts"
ethos. Layer with the **HITL approval** above (show the diff before it commits) for review. Optionally have her
commit to a side branch you merge. (Logseq graphs are commonly git-versioned already.) Coarser fallback for
broader/system changes: the system disk is **btrfs** → snapshots, but git is the right tool for journal/notes.

---

## Quick reference
```bash
eva "message"                                  # chat (keeper)
EVA_AGENT_ID=agent-5641… eva "message"         # chat the spicy test agent
scripts/sync-persona.sh agent-87f7dbff…        # push persona edits live
curl -s localhost:8283/v1/models/              # what models Letta sees
curl -s localhost:8283/v1/agents/              # list agents
systemctl --user status letta eva-web          # service health
```
Repo branch: `feat/eva-web-and-2026-review` (commits local, not pushed).
